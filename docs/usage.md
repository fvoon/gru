# Usage guide

> **Status**: TBA — to be authored by the `usage-doc` todo (see [`plan.md`](plan.md)).
>
> This guide will be the PM and engineer onboarding doc. It depends on the four skills under [`../.agents/skills/`](../.agents/skills) being authored first.

## Sections to author

1. **Prerequisites** (Cursor IDE, Atlassian MCP authentication, graphify install, `~/payments-graph/` setup).
2. **For PMs** — running `write-a-prd` end-to-end on a real feature idea.
3. **For PMs** — running `prd-to-jira-issues` on the parent ticket and reviewing slice boundaries (Gate A).
4. **For engineers** — running `spike-and-report` on a Research ticket; producing the Confluence research sub-page.
5. **For engineers** — `eng-reviewed` workflow (Gate B): reading the AI-optimized child description, checking acceptance criteria, transitioning the ticket.
6. **For anyone** — running `ai-ready-check` and interpreting pass/fail output.
7. **What success looks like** — concrete example of a green-path run end-to-end.
8. **Troubleshooting** — common failure modes (graphify stale, Atlassian MCP auth expired, missing Component, etc.).
9. **Filing issues with `gru` itself** — how and where.

(See [`plan.md`](plan.md) → "Demo plan" for the full target end-to-end walkthrough.)
