# Spike briefing — PLTPM-21500

> Read-only context for the agent. Do not edit this file. Append findings to `/Users/fvoon/IdeaProjects/gru/.agents/skills/spike-and-report/redlines/spike-PLTPM-21500-report.md` instead.

## Question (from PLTPM-21500)

**Spike: do we need a transactional outbox for wallet-to-wallet events?**

Investigate whether the new wallet-to-wallet event publisher requires a transactional outbox or whether the existing Axon event store guarantees suffice. Cover failure-mode analysis (publisher down, consumer down, network partition) and recommend an approach with a high-level migration path.

## Existing state (from graphify)

**Nodes returned by graphify (scoped to spike repos):**

- `WalletEventPublisher` — payment-platform (class)
- `WalletEventConsumer` — walletapi (class)
- `WalletEventStore` — payment-platform (class)

**Edges (cross-module relationships):**

- `WalletEventPublisher` --uses--> `WalletEventStore`
- `WalletEventPublisher` --publishes-to--> `WalletEventConsumer`

**Communities:**

- **Wallet event flow** — `WalletEventPublisher`, `WalletEventStore`, `WalletEventConsumer`

_Spike scoped to repos: payment-platform, walletapi._

## Worktrees

- `payment-platform` -> `/Users/fvoon/IdeaProjects/spike/PLTPM-21500/payment-platform`
- `walletapi` -> `/Users/fvoon/IdeaProjects/spike/PLTPM-21500/walletapi`

All on branch `spike/PLTPM-21500`, `push.default=nothing`. Trial commits are fine; PRs are not.

## Budget

- Wall-clock: ~30 minutes (soft).
- Turns: ~30 (LLM-self-policed; pivot to writing the report by turn 25).

## Where to write findings

Append to `/Users/fvoon/IdeaProjects/gru/.agents/skills/spike-and-report/redlines/spike-PLTPM-21500-report.md`. The 7-section template is already seeded with `TODO` placeholders; fill them in as you go. `write_spike_report.py` refuses to publish until every section is `TODO`-free.
