# Slice plan for PLTPM-99002: Transfers v3 ACH return reliability

> Generated by propose_slices.py from PLTPM-99002 + graphify on 2026-05-02T16:59Z.
> Edit freely: delete a `## Slice` section to drop, reorder slices to reorder, edit `depends_on:` to change blocking, edit prose. write_jira_children.py parses this file back into actions.

## Slice A — payment-platform: refactor TransferSaga return-handler chain into an explicit ReturnReceived aggregate event, add idempotency guard at the SQS consumer.
- type: Task
- title: [BE] refactor TransferSaga return-handler chain into an explicit ReturnReceived aggregate event, add idempotency guard at the SQS consumer.
- component: payment-platform
- depends_on: B
- modules: TransferSaga, SqsConsumer
- user_stories: 1, 2, 3
- description: |
  ## What to build (in payment-platform)
  
  refactor TransferSaga return-handler chain into an explicit ReturnReceived aggregate event, add idempotency guard at the SQS consumer.
  
  ## Background (auto-snapshot from parent — do not edit)
  
  The TransferSaga in payment-platform occasionally double-reverses the wallet ledger when Payliance pushes a return mid-saga. Wallet ledger correctness depends on saga + walletapi cooperation.
  
  **payment-platform impact**:
  - payment-platform: refactor TransferSaga return-handler chain into an explicit ReturnReceived aggregate event, add idempotency guard at the SQS consumer.
  
  ## Acceptance criteria
  
  <!-- TODO: PM/eng must fill in. write_jira_children.py refuses to write if any AC contains the literal `TODO`. -->
  - [ ] (testable criterion derived from user story 1)
  - [ ] (testable criterion derived from user story 2)
  - [ ] (testable criterion derived from user story 3)
  
  ## Implementation Hints (graphify-derived)
  
  - Modules to touch: TransferSaga, SqsConsumer
  
  ## Blocked by
  
  - Slice B
  
  ## User stories addressed
  
  - User story 1 (from parent PLTPM-99002)
  - User story 2 (from parent PLTPM-99002)
  - User story 3 (from parent PLTPM-99002)

## Slice B — walletapi: add deduplicated ledger-reversal entry point keyed on (transferId, returnReasonCode); reject duplicates at the API boundary.
- type: Task
- title: [BE] add deduplicated ledger-reversal entry point keyed on (transferId, returnReasonCode); reject duplicates at the API boundary.
- component: walletapi
- depends_on: (none)
- modules: WalletLedgerClient
- user_stories: 1, 2, 3
- description: |
  ## What to build (in walletapi)
  
  add deduplicated ledger-reversal entry point keyed on (transferId, returnReasonCode); reject duplicates at the API boundary.
  
  ## Background (auto-snapshot from parent — do not edit)
  
  The TransferSaga in payment-platform occasionally double-reverses the wallet ledger when Payliance pushes a return mid-saga. Wallet ledger correctness depends on saga + walletapi cooperation.
  
  **walletapi impact**:
  - walletapi: add deduplicated ledger-reversal entry point keyed on (transferId, returnReasonCode); reject duplicates at the API boundary.
  
  ## Acceptance criteria
  
  <!-- TODO: PM/eng must fill in. write_jira_children.py refuses to write if any AC contains the literal `TODO`. -->
  - [ ] (testable criterion derived from user story 1)
  - [ ] (testable criterion derived from user story 2)
  - [ ] (testable criterion derived from user story 3)
  
  ## Implementation Hints (graphify-derived)
  
  - Modules to touch: WalletLedgerClient
  
  ## Blocked by
  
  - None — can start immediately
  
  ## User stories addressed
  
  - User story 1 (from parent PLTPM-99002)
  - User story 2 (from parent PLTPM-99002)
  - User story 3 (from parent PLTPM-99002)

## Slice C — payment-platform: confirm SQS retry semantics for duplicate-suppression at the consumer boundary.
- type: Research
- title: Spike: confirm SQS retry semantics for duplicate-suppression at the consumer boundary.
- component: payment-platform
- depends_on: (none)
- modules: (unknown — fill from graphify)
- user_stories: (none)
- description: |
  ## Investigation
  
  confirm SQS retry semantics for duplicate-suppression at the consumer boundary.
  
  Document findings in a comment on this issue and link any follow-up tickets.
  
  ## Background (auto-snapshot from parent — do not edit)
  
  The TransferSaga in payment-platform occasionally double-reverses the wallet ledger when Payliance pushes a return mid-saga. Wallet ledger correctness depends on saga + walletapi cooperation.
  
  **payment-platform impact**:
  - payment-platform: refactor TransferSaga return-handler chain into an explicit ReturnReceived aggregate event, add idempotency guard at the SQS consumer.
  
  ## Acceptance criteria
  
  <!-- TODO: PM/eng must fill in. write_jira_children.py refuses to write if any AC contains the literal `TODO`. -->
  - [ ] (testable criterion 1)
  - [ ] (testable criterion 2)
  
  ## Implementation Hints (graphify-derived)
  
  - Modules to touch: (unknown — fill from graphify)
  
  ## Blocked by
  
  - None — can start immediately
  
  ## User stories addressed
  
  - (none — derived from parent PLTPM-99002)
