# Slice plan for PLTPM-99001: Merchant-scoped processor health

> Generated by propose_slices.py from PLTPM-99001 + graphify on 2026-05-02T16:59Z.
> Edit freely: delete a `## Slice` section to drop, reorder slices to reorder, edit `depends_on:` to change blocking, edit prose. write_jira_children.py parses this file back into actions.

## Slice A — payment-platform: extend ProcessorHealthClient with merchant-scoped lookup, add DEGRADED enum + reason-code list, plumb scope through the existing health cache.
- type: Task
- title: [BE] extend ProcessorHealthClient with merchant-scoped lookup, add DEGRADED enum + reason-code list, plumb scope through the existing health cache.
- component: payment-platform
- depends_on: (none)
- modules: ProcessorHealthClient, processor-health-cache
- user_stories: 1, 2, 3
- description: |
  ## What to build (in payment-platform)
  
  extend ProcessorHealthClient with merchant-scoped lookup, add DEGRADED enum + reason-code list, plumb scope through the existing health cache.
  
  ## Background (auto-snapshot from parent — do not edit)
  
  Today the processor-health endpoint reports a single global value per processor. Merchants cannot distinguish their own scope.
  
  **payment-platform impact**:
  - payment-platform: extend ProcessorHealthClient with merchant-scoped lookup, add DEGRADED enum + reason-code list, plumb scope through the existing health cache.
  
  ## Acceptance criteria
  
  <!-- TODO: PM/eng must fill in. write_jira_children.py refuses to write if any AC contains the literal `TODO`. -->
  - [ ] (testable criterion derived from user story 1)
  - [ ] (testable criterion derived from user story 2)
  - [ ] (testable criterion derived from user story 3)
  
  ## Implementation Hints (graphify-derived)
  
  - Modules to touch: ProcessorHealthClient, processor-health-cache
  
  ## Blocked by
  
  - None — can start immediately
  
  ## User stories addressed
  
  - User story 1 (from parent PLTPM-99001)
  - User story 2 (from parent PLTPM-99001)
  - User story 3 (from parent PLTPM-99001)
